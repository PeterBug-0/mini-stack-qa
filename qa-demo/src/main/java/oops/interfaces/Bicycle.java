package oops.interfaces;
// Interfaces define the contracts of a class, you can not define a concrete behaviour
//Interfaces are more restricted
public interface Bicycle {

    void applyBrake(int decrement);
    void speedUp (int increment);
}
